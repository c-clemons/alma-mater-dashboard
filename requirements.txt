# Upper bounds pin the tested majors. pandas 3.0 REMOVED DataFrame.applymap,
# which pages/monthly_pl_detail.py uses — so pandas must stay <3.
streamlit>=1.37.0,<2
pandas>=2.0.0,<3
numpy>=1.24.0,<3
plotly>=5.17.0,<7
python-dateutil>=2.8.2
openpyxl>=3.1.0,<4
reportlab>=4.0.0,<5
